# Config & Infrastructure Feature Specifications

Covers configuration validation, NATS message bus, and the state layer.

---

## 21. Config Validation

**Source**: `packages/shared/config/src/validation.ts`,
`packages/shared/config/src/loader.ts`, `packages/shared/config/src/main.ts`
**Test**: `packages/shared/config/src/validation.test.ts`,
`packages/shared/config/src/loader.test.ts`,
`packages/shared/config/src/main.test.ts` **Config**: `nachos.toml` (all
sections), `.env` (secrets)

### Behaviors

| ID    | Behavior                                                                                                                                                                                                                                                                                                                                                                          | Tested | Reference                                                                                                                              |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| CV-01 | TOML loading: `parseToml` parses valid TOML strings into `NachosConfig` objects, including nested sections, arrays, and `[[array.tables]]` entries                                                                                                                                                                                                                                | YES    | loader.test.ts:6-7, loader.test.ts:29, loader.test.ts:78, loader.test.ts:110                                                           |
| CV-02 | TOML loading: `parseToml` throws `ConfigLoadError` for malformed TOML syntax                                                                                                                                                                                                                                                                                                      | YES    | loader.test.ts:69-76                                                                                                                   |
| CV-03 | Config search: `loadConfig` searches `cwd/nachos.toml` then `~/.nachos/nachos.toml`; throws `ConfigLoadError` if no file is found or custom path does not exist                                                                                                                                                                                                                   | YES    | main.test.ts:73-77                                                                                                                     |
| CV-04 | CONFIG_SHAPE validation: `validateNoUnknownKeys` rejects any key not declared in the CONFIG_SHAPE tree (including nested objects and `__array` entries) and reports the full dotted path                                                                                                                                                                                          | YES    | validation.test.ts:249-262                                                                                                             |
| CV-05 | Required sections: validation rejects configs missing `[nachos]`, `[llm]`, or `[security]` sections; `[runtime]`, `[channels]`, and `[tools]` are optional                                                                                                                                                                                                                        | YES    | validation.test.ts:20-30, validation.test.ts:44-55, validation.test.ts:96-106                                                          |
| CV-06 | Value range enforcement: rejects `llm.max_tokens` outside 1..1,000,000; `llm.temperature` outside 0..2; `security.audit.retention_days` outside 1..365; rate limits below 1; webchat port outside 1..65535                                                                                                                                                                        | YES    | validation.test.ts:70-81, validation.test.ts:83-94, validation.test.ts:218-232, validation.test.ts:202-216, validation.test.ts:311-323 |
| CV-07 | Security cross-validation: `security.mode = "permissive"` requires `i_understand_the_risks = true`; `tools.shell.enabled = true` requires permissive mode; `tools.code_runner.runtime = "native"` requires permissive mode                                                                                                                                                        | YES    | validation.test.ts:121-132, validation.test.ts:134-144, validation.test.ts:146-158, validation.test.ts:160-171                         |
| CV-08 | Env var handling: `loadAndValidateConfig` loads `.env` file into `process.env` via dotenv by default; resolves `.env` path from `NACHOS_ENV_PATH`, config dir, or cwd; can be disabled with `applyDotenv: false`                                                                                                                                                                  | YES    | main.test.ts:100-124, main.test.ts:126-148                                                                                             |
| CV-09 | Docker WORKDIR relative path issue: `.env` resolution uses `path.join(path.dirname(configPath), '.env')` when a custom `configPath` is provided, or `path.join(process.cwd(), '.env')` otherwise. Inside Docker containers where WORKDIR differs from the config mount path, `NACHOS_ENV_PATH` env var must be set to the absolute path of the `.env` file for correct resolution | NO     | --                                                                                                                                     |
| CV-10 | Validation bypass: `loadAndValidateConfig({ validate: false })` skips `validateConfigOrThrow` and returns the raw parsed config even if it contains invalid values                                                                                                                                                                                                                | YES    | main.test.ts:52-71                                                                                                                     |

---

## 22. NATS Bus

**Source**: `packages/core/bus/src/client.ts`,
`packages/core/bus/src/topics.ts`, `packages/core/bus/src/types.ts` **Test**:
`packages/core/bus/src/client.test.ts`, `packages/core/bus/src/topics.test.ts`
**Config**: `runtime.redis_url` (for NATS URL passed at construction), NATS
token auth via `NachosBusOptions.token`

### Behaviors

| ID    | Behavior                                                                                                                                                                                                                                                       | Tested | Reference                                                                                                              |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------- |
| NB-01 | Connection: `connect()` calls `nats.connect` with servers (normalized to array), name, timeout (default 5000ms), maxReconnectAttempts (default 10), reconnectTimeWait (default 2000ms), and optional token; emits `connect` event on success                   | YES    | client.test.ts:101-111, client.test.ts:119-124                                                                         |
| NB-02 | Idempotent connect: calling `connect()` when already connected is a no-op; `nats.connect` is only invoked once                                                                                                                                                 | YES    | client.test.ts:113-117                                                                                                 |
| NB-03 | Disconnect: `disconnect()` drains then closes the NATS connection and nulls it; calling disconnect when not connected is a safe no-op                                                                                                                          | YES    | client.test.ts:128-138                                                                                                 |
| NB-04 | Publish: wraps payload in a `MessageEnvelope` (id, timestamp, source, type, correlationId) encoded as JSON via StringCodec; throws `"Not connected to NATS"` if not connected; supports custom type, correlationId, and reply subject                          | YES    | client.test.ts:152-153, client.test.ts:156-175, client.test.ts:177-199                                                 |
| NB-05 | Subscribe: creates NATS subscription on topic with optional queue group and max messages; returns `BusSubscription` with `unsubscribe()`, `drain()`, and `getSubscription()` methods; throws if not connected                                                  | YES    | client.test.ts:203-204, client.test.ts:207-216, client.test.ts:218-225, client.test.ts:227-232, client.test.ts:234-249 |
| NB-06 | Request/reply: sends envelope and awaits response decoded as `MessageEnvelope<TRes>`; uses configurable timeout (defaults to client timeout); converts NATS `Timeout` error code to descriptive `"Request to {topic} timed out after {ms}ms"` message          | YES    | client.test.ts:258-280, client.test.ts:282-305, client.test.ts:307-318                                                 |
| NB-07 | Health check: returns `unhealthy` with `connection: "error"` when not connected; returns `healthy` with `connection: "ok"` and measured flush latency when connected; returns `degraded` when flush fails                                                      | YES    | client.test.ts:322-346                                                                                                 |
| NB-08 | Event system: `on(event, handler)` registers handlers for `connect`, `disconnect`, `reconnect`, `error`, `update` events; `off(event, handler)` removes them; errors in handlers are caught and logged without crashing the client                             | YES    | client.test.ts:361-381                                                                                                 |
| NB-09 | Topic structure: all topics follow `nachos.<domain>.<component>.<action>` convention; dynamic topics use factory functions (e.g., `CHANNEL_TOPICS.inbound('slack')` returns `nachos.channel.slack.inbound`); wildcard topics use `*` for single-level matching | YES    | topics.test.ts:27-43, topics.test.ts:46-62, topics.test.ts:64-79                                                       |
| NB-10 | Topic extraction helpers: `extractChannelFromTopic`, `extractToolFromTopic`, and `extractSessionFromStreamTopic` parse topic strings via regex; return `null` for invalid or unrecognized topics                                                               | YES    | topics.test.ts:164-214                                                                                                 |

---

## 23. State Layer

**Source**: `packages/shared/state/src/state-layer.ts`,
`packages/shared/state/src/types.ts`,
`packages/shared/state/src/sessions/sqlite-sessions-store.ts`,
`packages/shared/state/src/session/redis-session-state-store.ts`,
`packages/shared/state/src/sessions/sessions-store-interface.ts` **Test**:
`packages/shared/state/src/state-layer.test.ts`,
`packages/shared/state/src/sessions/sqlite-sessions-store.test.ts` **Config**:
`runtime.state.*` (provider, filesystem.dir, postgres.connection_string),
`runtime.state.session.*` (provider, redis_url, ttl_seconds),
`runtime.state.sessions.*` (provider, sqlite.db_path,
postgres.connection_string)

### Behaviors

| ID    | Behavior                                                                                                                                                                                                                                                                                                                             | Tested | Reference                                                                                                                                                                                                                            |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SL-01 | Factory creation: `createStateLayer` instantiates all 6 store types (identity, bootstrap, memory, user-profile, session-state, sessions) based on provider config; sessions store is only created when `sessions` config is present, otherwise `sessionsStore` is `undefined`                                                        | YES    | state-layer.test.ts:178-183, state-layer.test.ts:185-202, state-layer.test.ts:204-209                                                                                                                                                |
| SL-02 | Filesystem providers: creates `FilesystemIdentityStore`, `FilesystemBootstrapStore`, `FilesystemMemoryStore`, `FilesystemUserProfileStore` when provider is `"filesystem"`; throws `ConfigError` if `dir` is not specified                                                                                                           | YES    | state-layer.test.ts:178-183, state-layer.test.ts:211-216                                                                                                                                                                             |
| SL-03 | Postgres providers: creates `PostgresIdentityStore`, `PostgresBootstrapStore`, `PostgresMemoryStore`, `PostgresUserProfileStore` when provider is `"postgres"`; throws `ConfigError` if `connectionString` is missing; deduplicates `pg.Pool` instances when multiple stores share the same connection string                        | YES    | state-layer.test.ts:218-225, state-layer.test.ts:227-236, state-layer.test.ts:238-255                                                                                                                                                |
| SL-04 | SQLite sessions store: creates tables (`sessions`, `messages`) with WAL journal mode and foreign keys on construction; enforces UNIQUE(channel, conversation_id) constraint; supports create, get, update, delete, list with filtering, pagination, archive/restore, and pin/unpin operations                                        | YES    | sqlite-sessions-store.test.ts:34-71, sqlite-sessions-store.test.ts:73-87, sqlite-sessions-store.test.ts:170-193, sqlite-sessions-store.test.ts:195-247, sqlite-sessions-store.test.ts:393-419, sqlite-sessions-store.test.ts:421-442 |
| SL-05 | Atomic get-or-create: `getOrCreateSessionAtomic` uses a SQLite transaction to atomically check for an existing session by (channel, conversationId) and create one if absent; returns `{ session, created: boolean }` to distinguish new vs existing                                                                                 | YES    | sqlite-sessions-store.test.ts:105-123                                                                                                                                                                                                |
| SL-06 | Message operations: `addMessage` inserts a message and updates session `last_activity` and `updated_at` in a single transaction; `getMessages` returns messages in chronological order with optional limit/offset; `replaceMessages` atomically deletes all existing messages and inserts replacements (used for context compaction) | YES    | sqlite-sessions-store.test.ts:249-335, sqlite-sessions-store.test.ts:368-391                                                                                                                                                         |
| SL-07 | Redis session state: `RedisSessionStateStore` lazily connects on first operation; supports TTL-based expiration on `set` and `touch`; uses key prefix `session:state:{sessionId}`; `InMemorySessionStateStore` is used as fallback when Redis is not configured, with a warning about data loss on restart                           | YES    | state-layer.test.ts:178-183 (factory test uses in-memory)                                                                                                                                                                            |
| SL-08 | Policy enforcement: every state operation calls `ensureAllowed` before executing; if `policyCheck` returns `{ allowed: false }`, operation throws `PolicyDeniedError` with the denial reason; operations with `context.internalTool = true` bypass the policy check entirely but are still audited                                   | YES    | state-layer.test.ts:284-296, state-layer.test.ts:298-316, state-layer.test.ts:318-331                                                                                                                                                |
| SL-09 | Audit logging: when an `auditLogger` dependency is provided, every state operation (both allowed and denied) is audited with action, outcome, userId, sessionId, channel, and securityMode; warns at construction if no policyCheck or auditLogger is provided                                                                       | YES    | state-layer.test.ts:333-355                                                                                                                                                                                                          |
| SL-10 | Graceful shutdown: `close()` iterates all stores and calls their `close()` methods if present, then ends all shared Postgres pools; `SqliteSessionsStore.close()` closes the underlying database connection, making further queries throw                                                                                            | YES    | state-layer.test.ts:259-265, sqlite-sessions-store.test.ts:531-540                                                                                                                                                                   |
